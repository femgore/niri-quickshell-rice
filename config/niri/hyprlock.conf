general {
    hide_cursor = true
}

background {
    monitor =
    color = rgba(14, 16, 20, 1.0)
}

input-field {
    monitor =
    size = 280, 56
    position = 0, -90
    halign = center
    valign = center
    outline_thickness = 2
    dots_size = 0.22
    dots_spacing = 0.18
    outer_color = rgba(255, 255, 255, 0.20)
    inner_color = rgba(20, 20, 24, 0.70)
    font_color = rgba(255, 255, 255, 0.90)
    fade_on_empty = false
    placeholder_text = Password
}

label {
    monitor =
    text = $TIME
    position = 0, 70
    halign = center
    valign = center
    font_size = 64
    color = rgba(255, 255, 255, 0.92)
}
